# BT Panel

轻量级服务器管理面板，使用 Go 语言开发。

## 功能特性

- 🌐 **网站管理** - Nginx 配置、SSL 证书自动申请
- 📁 **文件管理** - 在线文件浏览、编辑、上传、下载、压缩
- 💾 **数据库管理** - MySQL 数据库和用户管理
- 🔧 **软件管理** - OpenResty、PHP、MySQL、Redis 一键安装
- 👥 **FTP 管理** - FTP 用户创建和管理
- ⏰ **计划任务** - Cron 任务管理
- 🔥 **防火墙** - UFW 防火墙规则管理
- 💻 **Web 终端** - 浏览器内 SSH 终端
- 🔄 **自动更新** - 一键检测和升级新版本

## 一键安装

在服务器上执行以下命令（需要 root 权限）：

```bash
curl -sSL https://raw.githubusercontent.com/owen2026a/bt-releases/main/install.sh | sudo bash
```

或者：

```bash
wget -qO- https://raw.githubusercontent.com/owen2026a/bt-releases/main/install.sh | sudo bash
```

## 系统要求

- **操作系统**：Ubuntu 20.04+ / Debian 10+ / CentOS 7+
- **架构**：x86_64 (amd64)
- **内存**：≥ 512MB
- **磁盘**：≥ 1GB 可用空间

## 安装后

安装完成后，通过浏览器访问：

```
https://服务器IP:8099
```

**默认账号**：`admin`  
**默认密码**：`admin123`

> ⚠️ **重要**：首次登录后请立即修改默认密码！

## 常用命令

```bash
# 查看服务状态
systemctl status bt

# 重启服务
systemctl restart bt

# 停止服务
systemctl stop bt

# 查看日志
journalctl -u bt -f
```

## 目录结构

```
/www/bt/
├── bt              # 主程序
├── data/           # 数据目录（数据库）
├── backup/         # 升级备份
└── templates/      # 模板文件
```

## 卸载

```bash
# 停止并禁用服务
sudo systemctl stop bt
sudo systemctl disable bt

# 删除服务文件
sudo rm /etc/systemd/system/bt.service
sudo systemctl daemon-reload

# 删除应用目录（可选，会删除所有数据）
sudo rm -rf /www/bt
```

## 许可证

MIT License
